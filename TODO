- * Warning and Error reprots
   -> cannot find handler
- Commandline related (After new gnome is relased)
- Options:
	predefined aop parameters set

- Preview area:
	progress bar for rendering
	pick a background color	
	build preview from datum on RAM(done)
	variable width line that is supported from autotrace 0.31
	cleanup cancel
	DnD without temp file

- configure:

- Nautilus:
	Mime type for nautilus<0.2.5>	
	DnD Proxy<0.2.5>???
	Icon and DnD accepting and rejecting <0.2.4> (undone)

- Sodipodi:
	SVG Icon
- Gimp:
	check memory leak
	parameter and scriptable
	broken status (done)
	fill transparent region with specified background color(done)
	avoit to crash when the image size is changed(done)

Done:
- Image DnD
- frontline.m4
- gundo<0.2.4> 
- Dummy command line<0.2.4>
- Save format<0.2.4>
- Autotrace version check <0.2.4>
- Canvas<0.3.n> (done)
- GIMP:Preview default status: (Static color with black and hiding image)<done>
- preview zooming
- Limit number of splines
- reload<0.5.0> in GIMP plugin 
- drop to sodipodi as svg file<0.5.0>
- DnD porting as svg file
- Suffix options in file dialog
- Gettextize(http://larse-gtk.hp.infoseek.co.jp/gettext.html) (done)
- popt should be checked in configure
- imlib should be checked in configure
