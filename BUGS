Known bugs in frontline: -*-text-*-

- Options given through command line cause errors.

- Displaying an image and its trace result are failed 
   if the image has transparent area.

- Frontline menu item is not appeared on Gnome desktop menu.

- Lines that has variable width is not rendered well
  (not implemented).

- Errors occurred in autotrace is not messaged to the user.

